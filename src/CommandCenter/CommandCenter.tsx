import TextInput from "components/TextInput";
import { FC } from "react";

const CommandCenter:FC = () => {
    return (<div><TextInput id={'CommandInput'} label={'Command Input'} /></div>)
};

export default CommandCenter;